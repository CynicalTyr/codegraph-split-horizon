# Uninstall 00

Delete the two `codegraph.json` files this mod merged **only if you created the roots for the drill**. If you pointed at a real corpus, revert the JSON with git. Do not `rm -rf` `.codegraph/` unless you meant to drop that index.

Scratch drill:

```bash
rm -rf /tmp/horizon-op /tmp/horizon-arch
```
