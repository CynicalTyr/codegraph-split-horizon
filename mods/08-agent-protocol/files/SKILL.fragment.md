# CodeGraph split-horizon (any agent harness)

Install `@colbymchenry/codegraph` ≥ 1.6.0. This fragment is DLC policy, not his engine contribution guide.

- Pick the index: operator tree = default `serve --mcp --path`. Architect home = `projectPath` / `--path`. No live watcher on `projectPath`.
- `codegraph_explore` is the Read. About three calls per index. Do not Grep to confirm.
- Empty explore on scripts/docs → `codegraph files` then `node --file`. Never `language:bash`.
- MCP not connected → same CLI on that root.
- After `codegraph.json` change: `index --force`. After file edits on an unwatched root: `sync` as that DB's owner.
- Writer UID equals SQLite UID. Never spawn the indexer from an IDE hook.
- YAML hits are keys. Do not print values.
- Do not sit this session in the unattended operator inner loop.
