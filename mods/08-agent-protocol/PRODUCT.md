# Agent protocol

CodeGraph's own MCP instructions already say: trust explore, don't Grep to confirm, watch the staleness banner. Agents still open Glob. The extra split-horizon failure is **wrong root**. Explore on the architect index looks confident and still never saw the operator runtime.

Paste this rule where your harness reads policy (Cursor `.mdc`, Claude `CLAUDE.md` snippet, Codex `AGENTS.md` **in the operator repo**, not Colby's engine `AGENTS.md`).

Generator stays off the evaluator. Do not let the IDE session replace the unattended operator's inner loop. The local autonomous process keeps its own retrieval. The CLI IDE is out-of-band.

Ten-minute outcome: `HORIZON.rule.md`, `SKILL.fragment.md`, and `CLAUDE.md` in the operator `.horizon/` directory. You copy them. We do not clobber global rules or Colby's engine `AGENTS.md`.
